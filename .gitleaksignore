# gitleaks false-positive allowlist.
# One fingerprint per line: <commit>:<file>:<rule-id>:<start-line>
# Generated by running `gitleaks detect --source .` (full-history scan)
# against this repo on 2026-08-14 and manually confirming each entry below
# is a placeholder value, not a real credential.
#
# IMPORTANT: this file intentionally does NOT suppress every finding from
# that scan. A real AWS access key ID (rule: aws-access-token) was found in
# LYRA/LYRA-Handover.md (commit 3a7a8a795ea3bb583c2b7a2f9eae1e344f3f73aa) and
# is left un-ignored on purpose — it needs to be rotated and scrubbed, not
# hidden from future scans.

# .github/workflows/deploy.yml build job sets STRIPE_SECRET_KEY to the
# literal placeholder string "sk_test_placeholder" so `next build` has a
# non-empty value to work with; it is not a real Stripe key. Matches the
# gitleaks `stripe-access-token` rule on string shape alone.
c915ec9139690a96b5a7ca4a9a1a74205a3d6d85:.github/workflows/deploy.yml:stripe-access-token:67
3643aaeb1491864479a7790f72f5c57ca5f41259:LYRA/lyra/.github/workflows/deploy.yml:stripe-access-token:67
