AUTH0_DOMAIN=
AUTH0_MCP_AUDIENCE=
LYRA_API_BASE_URL=
APP_BASE_URL=
PORT=3100
REDIS_URL=

# Dev-only. Used exclusively by `npm run eval` (scripts/tool-selection-eval.ts)
# to make real Claude API calls when scoring tool-selection accuracy. Never
# read by the deployed gateway server itself -- not required for `npm run dev`
# or production.
ANTHROPIC_API_KEY=
